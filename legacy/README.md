# Legacy Python tracker

The original Google Sheets viewer. Run locally from this folder:

```bash
python serve.py
```

Dev reload:

```bash
python serve_dev.py
```

The live Vercel deploy used `vercel.json` in this folder. The repo root is now the Vite + React + Supabase app.
